
<!DOCTYPE html>
<html lang="vi">
<head>
    <meta charset="UTF-8">
    <title>Danh sách khối</title>

    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet">
    <link href="https://fonts.googleapis.com/css2?family=Nunito:wght@400;600;700;800&display=swap" rel="stylesheet">

    <script src="https://code.jquery.com/jquery-3.6.0.min.js"></script>

    <style>
        body {
        font-family: 'Nunito', sans-serif;
        background: #f4f7fb;
        }

        /* HEADER */
        .navbar {
        background: #fff;
        border-radius: 16px;
        margin: 15px;
        padding: 10px 20px;
        }

        .logo {
        font-weight: 800;
        color: #ff7a00;
        }

        /* TITLE */
        .page-title {
        margin: 20px;
        font-weight: 800;
        }

        /* CARD */
        .grade-card {
        border-radius: 20px;
        padding: 20px;
        color: #fff;
        height: 160px;
        position: relative;
        transition: 0.3s;
        }

        .grade-card:hover {
        transform: translateY(-5px);
        }

        /* COLORS */
        .bg-blue {
        background: linear-gradient(135deg, #4facfe, #00c6ff);
        }

        .bg-green {
        background: linear-gradient(135deg, #43e97b, #38f9d7);
        }

        .bg-purple {
        background: linear-gradient(135deg, #a18cd1, #fbc2eb);
        }

        /* TEXT */
        .grade-title {
        font-size: 22px;
        font-weight: 800;
        }

        .grade-info {
        font-size: 14px;
        margin-top: 5px;
        }

        /* BUTTON */
        .btn-view {
        position: absolute;
        bottom: 15px;
        background: #fff;
        color: #333;
        border-radius: 20px;
        font-weight: 700;
        padding: 6px 14px;
        }

        /* ICON */
        .grade-icon {
        position: absolute;
        right: 10px;
        bottom: 10px;
        width: 70px;
        opacity: 0.8;
        }
    </style>
</head>

<body>

    <!-- HEADER -->
    <nav class="navbar d-flex justify-content-between">
        <div class="logo">MSKA - Giáo viên</div>

        <div>
            <span class="me-3">Nguyễn Văn A</span>
            <img src="https://i.pravatar.cc/40" class="rounded-circle">
        </div>
    </nav>

    <!-- TITLE -->
    <h4 class="page-title">Danh sách khối</h4>

    <!-- LIST -->
    <div class="container">
        <div class="row" id="gradeList"></div>
    </div>

    <script>
        const grades = [
        { name: "Khối 1", classes: 3, students: 90, color: "bg-blue" },
        { name: "Khối 2", classes: 4, students: 120, color: "bg-green" },
        { name: "Khối 3", classes: 2, students: 60, color: "bg-purple" },
        { name: "Khối 4", classes: 3, students: 85, color: "bg-blue" },
        { name: "Khối 5", classes: 2, students: 70, color: "bg-green" }
        ];

        function renderGrades() {
        let html = "";

        grades.forEach(g => {
        html += `
        <div class="col-md-4 mb-4">
        <div class="grade-card ${g.color}">
        <div class="grade-title">${g.name}</div>
        <div class="grade-info">Số lớp: ${g.classes}</div>
        <div class="grade-info">Học sinh: ${g.students}</div>

        <button class="btn btn-view">Xem chi tiết</button>

        <img src="https://cdn-icons-png.flaticon.com/512/3135/3135755.png" class="grade-icon">
        </div>
        </div>
        `;
        });

        $("#gradeList").html(html);
        }

        $(document).ready(function () {
        renderGrades();
        });
    </script>

</body>
</html>
